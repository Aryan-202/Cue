package com.music.cue.org.presentation.screens.player

enum class CuePlayerState {
    STATE_IDLE,
    STATE_BUFFERING,
    STATE_READY,
    STATE_PLAYING,
    STATE_ERROR,
    STATE_END,
    STATE_PAUSE,
    STATE_NEXT_TRACK
}